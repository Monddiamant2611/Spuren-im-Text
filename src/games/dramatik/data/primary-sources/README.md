# Redaktionell geschützte Primärtexte

Hier dürfen ausschließlich redaktionell freigegebene JSON-Dateien liegen. Phase 0 enthält bewusst keinen Primärtext. Nach Freigabe wird für jede Datei ein SHA-256-Wert in `integrity-manifest.json` eingetragen.
