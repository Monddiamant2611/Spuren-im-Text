import { AssetImage } from "../../../shared/components/AssetImage";
export type Decoration = { asset: string; position: "top-left" | "top-right" | "bottom-left" | "bottom-right"; width?: string; opacity?: number; rotation?: number; zIndex?: number; visibility?: "desktop" | "tablet-up" | "all"; flipX?: boolean };
export function DecorativeLayer({ decorations }: { decorations: readonly Decoration[] }) { return <div className="decorative-layer" aria-hidden="true">{decorations.map((item, index) => <span key={`${item.asset}-${index}`} className={`decoration decoration--${item.position} decoration--${item.visibility ?? "all"}`} style={{ width: item.width, opacity: item.opacity, transform: `rotate(${item.rotation ?? 0}deg) scaleX(${item.flipX ? -1 : 1})`, zIndex: item.zIndex }}><AssetImage id={item.asset} decorative fallback={null} /></span>)}</div>; }
