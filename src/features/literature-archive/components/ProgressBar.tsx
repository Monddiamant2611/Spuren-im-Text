export function ProgressBar({ value, max, label }: { value: number; max: number; label: string }) { const percentage = Math.max(0, Math.min(100, (value / max) * 100)); return <div className="progress"><div className="progress__labels"><span>{label}</span><span>{value} von {max}</span></div><div className="progress__track" role="progressbar" aria-valuemin={0} aria-valuemax={max} aria-valuenow={value} aria-label={label}><span style={{ width: `${percentage}%` }} /></div></div>; }
